/** @format */
const steps = document.querySelectorAll('.steps');
const miles = document.querySelectorAll('.miles');
const sleep = document.querySelectorAll('.sleep');
const calories = document.querySelectorAll('.calories');
const exercise = document.querySelectorAll('.exercise');

let stuff = {
	steps: steps.innerText,
	miles: miles.innerText,
	sleep: sleep.innerText,
	calories: calories.innerText,
	exercise: exercise.innerText,
};
